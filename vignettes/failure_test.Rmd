---
title: "R Notebook"
output: html_notebook
---

```{r setup}
devtools::load_all()
```

```{r}
pv <- list(param = expand.grid(a = 1:3, b = 4:6))

.func <- function (param, value) {
    condition <- param$a == 2
    if (condition) {
       #stop('Deliberate failure')
    }
    
    list(foo = condition)
}

result <- pv_call(.func, pv)
```

